# 팔로업 노트

> 집↔회사 이어서 작업하기 위한 세션 인수인계. 최신 세션 내용을 위에 append.

---

## 세션 — 계약 탭 개선 (집에서 작업, ~v4.13.1)

### 환경 (집 PC에서 새로 세팅함)
- Node.js LTS 설치, `npm install` 완료
- git 연결됨 (`origin/main` 추적). 작업 흐름: **작업 시작 전 `git pull` → 작업 → 커밋 → push** (main 직접, Vercel 자동 배포)
- **커밋 컨벤션·로컬 실행법은 [CLAUDE.md](CLAUDE.md) 참고** (이번에 문서화함)
- 로컬 실행:
  ```
  npm run dev                  # 프론트 5173
  vercel dev --listen 5601     # 백엔드(계약/행정 API) 5601
  ```
- 계약 탭 백엔드는 `NOTION_TOKEN`이 Vercel **Development 스코프**에 있어야 로컬에서 동작 (이번에 추가함). 값은 Sensitive라 `vercel env pull`로 안 나옴 → 원본(Notion 인테그레이션 시크릿)에서 가져와야 함.

### 오늘 반영한 것 (커밋됨)
1. **계약 탭 순차 아코디언 흐름** (v4.12.0) — 진행 단계 인라인 편집, 완료는 접힘/모달 수정, 대기는 소프트 잠금
2. **추가 버튼 UX 통일** (v4.12.1) — 동일 아웃라인 필, 맥락상 다음 액션만 앰버 강조, 파트너사 버튼 사이드바 하단
3. **자동갱신 반영** (v4.12.2) — 실제 계약서 Article 7에 180일 자동갱신 조항 확인. 자동갱신 계약은 만료 임박 경보(사이드바/카운트/Slack)에서 제외, 행엔 `↻ 자동갱신` 정보 표시
4. **서류 구글드라이브 링크** (v4.13.0) — 서류별 드라이브 링크 필드 + 열기 버튼, 링크 넣으면 받음 체크 자동. 노션 DB에 URL 속성 5개 추가
5. **파트너 공통 서류 연동** (v4.13.1) — 법인등록증·법인통장은 거래처등록에서만 입력, 지출기안은 읽기전용 `거래처등록 연동` 표시. 카운트/경고/Slack도 거래처등록 상태를 따름

### 🔜 다음에 할 것 (미결)

- [ ] **[중요/결정 필요] 프로젝트 시작 프로세스: 부속합의서 → 프로토타입 지출기안 순서**
  - 사용자 지침: "프로젝트 시작할 때 **부속합의서 계약이 먼저** 있어야 하고, 그 다음에 프로토타입 지출기안을 요청."
  - **현재 코드와 충돌**: `src/components/Contracts.jsx`의 `createProjectTemplate()`은 지금 **첫 프로젝트는 지출기안만** 생성하고 부속합의서를 안 만듦 (2번째 프로젝트부터 부속합의서 포함). 이건 이전 커밋 `5effceb`의 의도적 결정("첫 프로젝트는 본계약에 프로토타입 포함")이었음.
  - **결정할 것**: 첫 프로젝트도 부속합의서를 먼저 만들어야 하나? (지침대로면 예 → `createProjectTemplate`에서 `isFirst`여도 부속합의서 포함하도록 수정)
  - 참고: 그룹 내 표시 순서(부속합의서→지출기안)는 이미 `KIND_ORDER`로 맞춰져 있음. 쟁점은 "첫 프로젝트에 부속합의서 항목을 자동 생성하느냐"임.

- [ ] (선택) "완료하고 다음 단계" 눌렀을 때 다음 단계로 스크롤/애니메이션 이동 등 마무리 디테일
- [ ] (선택) 서류 구글드라이브 **자동 업로드**(2단계) — 지금은 링크 붙이기만. 필요 시 Google Drive API(서비스계정) 연동으로 확장. 링크 필드가 그대로 업로드 저장소가 됨

### 참고
- 배포 URL: https://my-dashboard-gamma-amber.vercel.app
- 계약 데이터 노션 DB: `519164c1-6c91-4567-9daf-ce69b6d9ab58`
